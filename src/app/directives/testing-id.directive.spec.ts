import { TestingIdDirective } from './testing-id.directive';

describe('TestingIdDirective', () => {
  it('should create an instance', () => {
    const directive = new TestingIdDirective();
    expect(directive).toBeTruthy();
  });
});
