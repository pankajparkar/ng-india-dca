import { InvalidFieldFocusDirective } from './invalid-field-focus.directive';

describe('InvalidFieldFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidFieldFocusDirective();
    expect(directive).toBeTruthy();
  });
});
